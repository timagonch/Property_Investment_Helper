[project]
name = "property-investment-helper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "altair>=5.0",
    "dotenv>=0.9.9",
    "geopandas>=1.1.3",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.8",
    "nbconvert>=7.17.0",
    "numpy>=2.4.2",
    "pandas>=2.2.0,<3",
    "plotly>=6.6.0",
    "pyshp>=3.0.3",
    "requests>=2.32.5",
    "scikit-learn>=1.8.0",
    "seaborn>=0.13.2",
    "streamlit>=1.40.0",
    # shap>=0.51: install manually with `uv pip install shap` (cross-platform lock
    # conflict — shap requires numba<0.63 on macOS x86_64, no Python 3.12 wheel)
    "streamlit-option-menu>=0.4.0",
]
